# A tuple is a data structure (container to store different values)

coordinates = (4, 5)  # x, y coordinates
# Cannot modify tuples at all. They're set in stone
# Note the difference. Tuples are in parantheses, lists are in brackets

coordinate_list = [(1, 2), (3, 4), (5, 6)]  # A list of tuples
print(coordinate_list)
